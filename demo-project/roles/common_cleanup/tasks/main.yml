---
- name: Remove demo packages
  ansible.builtin.package:
    name: "{{ common_packages }}"
    state: absent

- name: Remove MOTD
  ansible.builtin.copy:
    mode: "0644"
    content: ""
    dest: /etc/motd
...
