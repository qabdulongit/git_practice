---
- name: Install common packages
  ansible.builtin.package:
    name: "{{ common_packages }}"
    state: present

- name: Deploy MOTD
  ansible.builtin.template:
    mode: "0644"
    src: motd.j2
    dest: /etc/motd
...
